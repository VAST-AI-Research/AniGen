# AniGen Apple Silicon inference environment — Python 3.10
# torch>=2.11 required on Darwin for flex_gemm (at::mps::dispatch_sync_with_rethrow).
# CUDA-only wheels (spconv-cuXX, nvdiffrast, flash-attn, CUDA pytorch3d) are intentionally absent.
torch==2.12.0
torchvision==0.27.0
numpy>=1.24,<2
scipy==1.15.3
transformers==5.9.0
safetensors==0.7.0
huggingface-hub==1.16.4
einops==0.8.2
trimesh==4.10.1
rtree==1.4.1             # trimesh.proximity.closest_point spatial index (skin-weight transfer)
xatlas==0.0.11
pymeshfix
pyvista
python-igraph
opencv-python
pillow
imageio
tqdm
omegaconf
easydict
scikit-learn>=1.5,<1.6   # skin_utils PCA; <1.6 keeps numpy<2 compatibility
pygltflib==1.16.5        # GLB export (export_utils) — pure Python
plyfile==1.0.3           # utils3d dep; >=1.1 forces numpy>=2, so pin to last numpy<2-compatible release
# Background removal (image_utils). rembg pulls onnxruntime (runs the U2Net ONNX
# model) and opencv-python-headless. opencv-python-headless>=4.12 forces numpy>=2,
# so pin <4.12 to preserve the numpy<2 constraint above.
rembg==2.0.69
onnxruntime==1.23.2
opencv-python-headless<4.12
# DSINE surface-normal model (loaded via torch.hub) depends on geffnet (EfficientNet backbone).
geffnet==1.0.2
# AniGen pins utils3d 0.0.2 (its postprocessing uses rasterize_triangle_faces /
# compute_dual_graph etc.); NOT Pixal3D's 1.3. nvdiffrast (hard-imported by
# utils3d.torch.rasterization) is aliased to mtldiffrast in anigen_mps bootstrap.
utils3d @ git+https://github.com/EasternJournalist/utils3d.git@9a4eb15e4021b67b12c460c7057d642626897ec8
